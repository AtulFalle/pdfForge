# web/

Angular 21 editor lives here in **Phase 3**. Do not run `ng new` until the Rust API is reviewed and accepted.

The UI will render PDFs with pdf.js and send edits to the Axum API. It must not parse or rewrite PDF internals.
